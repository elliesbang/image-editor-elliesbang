export const onRequestPost = async ({ request, env }) => {
  try {
    const { imageBase64 } = await request.json();

    if (!imageBase64) {
      return new Response(
        JSON.stringify({ error: "이미지 데이터가 없습니다." }),
        { status: 400, headers: { "Content-Type": "application/json" } }
      );
    }

    // Base64 → Binary 변환
    const base64Clean = imageBase64.replace(/^data:image\/\w+;base64,/, "");
    const imageBytes = Uint8Array.from(atob(base64Clean), (c) =>
      c.charCodeAt(0)
    );

    // ✅ Florence-2 Auto-Crop (Gradio Space 방식)
    //  ※ 실제 Space 주소: https://microsoft-florence-2-auto-crop.hf.space/run/predict
    const formData = new FormData();
    const blob = new Blob([imageBytes], { type: "image/png" });
    formData.append("image", blob, "input.png");

    const response = await fetch(
      "https://microsoft-florence-2-auto-crop.hf.space/run/predict",
      {
        method: "POST",
        body: formData, // ✅ multipart/form-data 자동 설정
      }
    );

    if (!response.ok) {
      const errText = await response.text();
      throw new Error(
        `Florence-2 Space API 오류 (${response.status}): ${errText}`
      );
    }

    // ✅ Space 응답 (JSON)
    const result = await response.json();

    // 결과 base64 추출
    const outputBase64 = result.data?.[0];
    if (!outputBase64 || !outputBase64.startsWith("data:image")) {
      throw new Error("크롭된 결과 이미지를 찾을 수 없습니다.");
    }

    // Base64 → Binary 변환 후 반환
    const outputBuffer = Uint8Array.from(
      atob(outputBase64.split(",")[1]),
      (c) => c.charCodeAt(0)
    );

    return new Response(outputBuffer, {
      headers: { "Content-Type": "image/png" },
    });
  } catch (err) {
    console.error("🚨 Florence-2 Auto-Crop 오류:", err);
    return new Response(JSON.stringify({ error: err.message }), {
      status: 500,
      headers: { "Content-Type": "application/json" },
    });
  }
};
